language: android
jdk: oraclejdk8
dist: trusty

env:
  global:

android:
  components:
   - tools
   - platform-tools
   - build-tools-30.0.3
   - android-30

   licenses:

before_install:
- chmod +x gradlew
- yes | sdkmanager "platforms:android-30"

before_script: